export interface Config {
  basePath: string;
  openApiPath: string;
  port: number;
  proxyEnabled: boolean;
  proxyUrl: string;
  routePrefix: string;
}

export const DUMMY_EXPORT_FOR_TEST_COVERAGE = 1;
