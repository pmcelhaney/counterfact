---
"counterfact": minor
---

Add support for a `counterfact.yaml` config file. All CLI options can now be specified in a `counterfact.yaml` file in the current working directory. Command-line options always take precedence over config file settings. Use `--config <path>` to load a config file from a non-default location.
