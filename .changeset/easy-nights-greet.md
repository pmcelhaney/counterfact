---
"counterfact": minor
---

Add support for OpenAPI 3.2 `querystring` parameter location: generates a typed `$.querystring` property on the route handler argument and populates it at runtime with the full parsed query string
